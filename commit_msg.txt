feat: Add diagnostic logging for temperature source

Adds detailed DEBUG level logging to trace the source of the `current_temperature` value, helping diagnose whether the internal sensor (`TemSen`) or a configured external sensor is being used. Also adjusts existing logging levels for clarity.

Changes:
- Added DEBUG logs to `climate.py` (`current_temperature`, `_async_update_current_temp`) and `climate_helpers.py` (`get_internal_temp`).
- Adjusted existing logging levels in `climate.py` (INFO->DEBUG, removed redundant DEBUG).
- Fixed indentation errors in `climate_helpers.py`.